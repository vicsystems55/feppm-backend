# Archived MySQL migration history

These migrations were applied to the original XAMPP and Clever Cloud MySQL databases.
They are retained for audit, rollback planning, and the MySQL-to-PostgreSQL data transfer.

Do not run these files against PostgreSQL or Supabase. The active PostgreSQL migration
history is in `prisma/migrations`.
